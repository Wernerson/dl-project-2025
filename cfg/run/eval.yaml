# @package _global_
# overrides for evaluation

checkpoint: outputs/ckpts/one-note.ckpt

logger:
  offline: true
  log_model: false

trainer:
  fast_dev_run: true
  callbacks: # no callbacks in eval runs

paths:
  sample_dir: ./outputs/samples