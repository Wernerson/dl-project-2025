# @package _global_
# overrides for a cluster run

paths:
  # override data & eval storage paths
  data_dir: ${oc.env:TMPDIR}/data
  eval_dir: ${oc.env:TMPDIR}/eval

  # override cash for FMD metric
  fmd_cache_dir: "/work/scratch/${oc.env:USER}/.cache/frechet_music_distance/checkpoints/clamp2"