# @package _global_
defaults:
  - random  # Inherits all settings from your original experiment

experiment_name: random_masking_gidd

# Enable GIDD Training (Uniform Noise)
model:
  error_correct: True

# Enable GIDD Evaluation (Self-Correction Step)
evaluator:
  use_self_correction: True