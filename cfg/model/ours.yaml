_target_: model.ours.OurModel

optimizer:
  _target_: torch.optim.Adam
  _partial_: true
  lr: 0.0005

net:
  _target_: net.transformer.OneHot
  in_dim: 4
  hidden_dim: 256
  out_dim: 128
  num_heads: 4
  transformer_layers: 6

criterion:
  _target_: net.transformer.OneHot.loss
  _partial_: true
